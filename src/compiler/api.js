const api = {
  print: () => {

    // if (templete.match(/%s/g)[0].length !== params.length) {
    //   throw `[Error]print: template and params length not match`
    // }
  }  
}

module.exports = api